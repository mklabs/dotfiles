# Load all the `.bash` files in `.dotfiles` topics

for i in ~/.dotfiles/**/*.sh; do
  if [ -e $i ]; then
    source $i
  fi
done

export NVM_DIR="/home/mk/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
